import React, { useContext } from "react";

import { ThemeContext } from '../contexts/ThemeContext';

export default function Button() {
  // const { setQualquer } = useContext(ThemeContext);

  return (
    <button onClick={() => console.log()}>asd</button>
  );
}
